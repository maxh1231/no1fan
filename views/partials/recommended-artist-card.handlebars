<div id="recommended-artist-card" class="m-2 p-2 border curso-pointer">
    <div id="recommended-id" class="hidden">{{myRecommendation.id}}</div>
    <div class="text-center">Like {{name}} ? You might like {{myRecommendation.name}}</div>
</div>